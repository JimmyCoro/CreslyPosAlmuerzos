{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && cd poscresly && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd poscresly && python manage.py migrate --noinput && python manage.py crear_superusuario && daphne -b 0.0.0.0 -p $PORT poscresly.asgi:application",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

